# CORE Terminology Glossary

Combined glossary for CORE-02 (instance-aware status), CORE-03/04
(lease schema and recovery), and CORE-05/06 (dispatch telemetry and
noop diagnostics). Each term includes its source milestone, type,
and a usage example.

---

## CORE-02: Instance-Aware Status Terms

### agent_instances

| Field | Detail |
|-------|--------|
| Milestone | CORE-02 |
| Type | array of objects |
| Description | List of all registered agent instances in the orchestrator pool. Each entry contains identity, status, and project metadata. |

**Example:**
```json
[
  { "instance_id": "claude_code#worker-01", "status": "active", "verified": true },
  { "instance_id": "gemini#worker-01", "status": "stale", "verified": true }
]
```

---

### active_agent_identities

| Field | Detail |
|-------|--------|
| Milestone | CORE-02 |
| Type | array of objects |
| Description | Subset of `agent_instances` filtered to only those with `status: "active"`. Used by the manager to determine which agents are available for dispatch. |

**Example:**
```json
[
  { "instance_id": "claude_code#worker-01", "verified": true, "same_project": true }
]
```

---

### instance_id

| Field | Detail |
|-------|--------|
| Milestone | CORE-02 |
| Type | string |
| Format | `{agent_name}#{instance_suffix}` |
| Description | Unique identifier for a single running instance of an agent. Combines the base agent name with an instance-specific suffix to distinguish multiple workers of the same agent type. |

**Example:** `claude_code#worker-01`, `gemini#worker-02`, `codex#leader`

---

### verified

| Field | Detail |
|-------|--------|
| Milestone | CORE-02 |
| Type | boolean |
| Description | Whether the agent instance has completed identity verification via `orchestrator_connect_to_leader`. An unverified instance can register but cannot claim tasks or receive dispatches. |

**Example:** `true` after successful connection handshake; `false` for a newly registered but unverified agent.

---

### same_project

| Field | Detail |
|-------|--------|
| Milestone | CORE-02 |
| Type | boolean |
| Description | Whether the agent instance is working on the same project (matching `cwd` / project root) as the manager. Agents on different projects are excluded from task assignment. |

**Example:** `true` when agent's `cwd` is `/Users/alex/claude-multi-ai` and manager's project root matches.

---

### heartbeat_timeout_minutes

| Field | Detail |
|-------|--------|
| Milestone | CORE-02 |
| Type | integer |
| Default | 10 |
| Description | Number of minutes after the last heartbeat before an agent is marked `stale`. Configurable per deployment. The manager cycle checks `last_seen` against this threshold. |

**Example:** With `heartbeat_timeout_minutes: 10`, an agent last seen at 00:05 becomes stale at 00:15.

---

### status (agent)

| Field | Detail |
|-------|--------|
| Milestone | CORE-02 |
| Type | string enum |
| Values | `active`, `offline`, `stale` |
| Description | Current lifecycle state of an agent instance, derived from heartbeat recency. |

| Value | Meaning |
|-------|---------|
| `active` | Heartbeat received within `heartbeat_timeout_minutes`. Agent is operational. |
| `offline` | Agent has explicitly disconnected or never completed connection. |
| `stale` | Heartbeat age exceeds threshold but no explicit disconnect. Agent may be hung. |

**Example:** An agent that last heartbeated 15 minutes ago with a 10-minute timeout has status `stale`.

---

## CORE-03/04: Lease Schema and Recovery Terms

### lease

| Field | Detail |
|-------|--------|
| Milestone | CORE-03 |
| Type | object |
| Description | A time-bounded claim on a task. When an agent claims a task, the orchestrator issues a lease granting exclusive ownership for a limited duration. The lease must be renewed periodically or it expires. |

**Example:**
```json
{
  "lease_id": "lease-20260226-001",
  "task_id": "TASK-abcdef12",
  "owner_instance_id": "claude_code#worker-01",
  "issued_at": "2026-02-26T00:10:00Z",
  "expires_at": "2026-02-26T00:20:00Z",
  "ttl_seconds": 600,
  "attempt_index": 1
}
```

---

### lease_id

| Field | Detail |
|-------|--------|
| Milestone | CORE-03 |
| Type | string |
| Description | Unique identifier for a specific lease issuance. Generated by the engine at claim time. A new `lease_id` is created each time a task is leased, even if it is the same task being re-leased after expiry. |

**Example:** `lease-20260226-001`

---

### owner_instance_id

| Field | Detail |
|-------|--------|
| Milestone | CORE-03 |
| Type | string |
| Description | The `instance_id` (from CORE-02) of the agent that holds the lease. Links the lease system to instance-aware identity. Used by expiry recovery to determine which agent's work to requeue. |

**Example:** `claude_code#worker-01` -- the specific instance, not just the base agent name.

---

### issued_at

| Field | Detail |
|-------|--------|
| Milestone | CORE-03 |
| Type | ISO 8601 string |
| Description | Timestamp when the lease was first created. Set once at claim time and never updated. Used to compute lease age and audit claim history. |

**Example:** `2026-02-26T00:10:00Z`

---

### expires_at

| Field | Detail |
|-------|--------|
| Milestone | CORE-03 |
| Type | ISO 8601 string |
| Description | Timestamp when the lease becomes invalid if not renewed. Computed as `issued_at + ttl_seconds` on initial claim, or `renewed_at + ttl_seconds` after renewal. The manager cycle compares this to the current time to detect expired leases. |

**Example:** `2026-02-26T00:20:00Z` (10 minutes after issuance with 600s TTL)

---

### renewed_at

| Field | Detail |
|-------|--------|
| Milestone | CORE-03 |
| Type | ISO 8601 string or null |
| Description | Timestamp of the most recent lease renewal. Null if the lease has never been renewed. Each renewal resets `expires_at` to `renewed_at + ttl_seconds`. |

**Example:** `2026-02-26T00:15:00Z` after a mid-lease renewal; `null` if never renewed.

---

### ttl_seconds

| Field | Detail |
|-------|--------|
| Milestone | CORE-03 |
| Type | integer |
| Default | 600 |
| Description | Time-to-live for the lease in seconds. Determines how far into the future `expires_at` is set from the issuance or last renewal. Configurable per deployment. |

**Example:** `600` (10 minutes). A lease issued at 00:10 with TTL 600 expires at 00:20.

---

### attempt_index

| Field | Detail |
|-------|--------|
| Milestone | CORE-03 / CORE-04 |
| Type | integer |
| Description | Count of how many times this task has been leased. Starts at 1 for the first claim. Incremented each time the task is re-leased after an expiry recovery. Used to enforce `max_retries`. |

**Example:** `1` on first claim, `2` after one expiry and re-lease, `3` after two.

---

### lease recovery

| Field | Detail |
|-------|--------|
| Milestone | CORE-04 |
| Type | process |
| Description | The manager-cycle step that scans for expired leases (`expires_at < now`) and requeues the associated tasks to `assigned` status. Increments `attempt_index` and clears the stale lease. Runs idempotently to prevent double-requeue. |

**Example:** Manager cycle at 00:25 finds a lease that expired at 00:20. The task moves from `in_progress` back to `assigned` with `attempt_index` incremented from 1 to 2.

---

### requeue

| Field | Detail |
|-------|--------|
| Milestone | CORE-04 |
| Type | action |
| Description | Moving an expired or blocked task back to `assigned` status so it can be claimed by another agent. Part of lease recovery. The requeued task retains its original metadata but gets a fresh lease on the next claim. |

**Example:** Task TASK-abcdef12 is requeued after its lease expired. It becomes available for any active agent to claim via `orchestrator_claim_next_task`.

---

## CORE-05/06: Dispatch Telemetry and Noop Diagnostics Terms

### dispatch

| Field | Detail |
|-------|--------|
| Milestone | CORE-05 |
| Type | concept |
| Description | The deterministic command-and-response pattern used by the manager to assign work to agents. Replaces implicit "publish and hope" with explicit command/ack/noop flows linked by correlation IDs. |

**Example:** Manager sends `dispatch.command` to `claude_code#worker-01`; worker responds with `dispatch.ack`; if no ack within timeout, a `dispatch.noop` is generated.

---

### correlation_id

| Field | Detail |
|-------|--------|
| Milestone | CORE-05 |
| Type | string |
| Description | Unique identifier linking a `dispatch.command` to its `dispatch.ack` or `dispatch.noop` response. Enables end-to-end tracing of a single dispatch cycle through logs and events. |

**Example:** `cmd-20260226-001` -- present in the command, echoed in the ack or noop.

---

### dispatch.command

| Field | Detail |
|-------|--------|
| Milestone | CORE-05 |
| Type | event type |
| Description | Event emitted by the manager to instruct a specific agent to perform an action (e.g., claim a task). Contains `correlation_id`, `target`, `action`, and `timeout_seconds`. |

**Example:**
```json
{
  "type": "dispatch.command",
  "correlation_id": "cmd-20260226-001",
  "source": "codex",
  "target": "claude_code#worker-01",
  "action": "claim_next",
  "timeout_seconds": 120
}
```

---

### dispatch.ack

| Field | Detail |
|-------|--------|
| Milestone | CORE-05 |
| Type | event type |
| Description | Event emitted by a worker to confirm receipt and processing of a `dispatch.command`. Echoes the `correlation_id` to close the dispatch loop. |

**Example:**
```json
{
  "type": "dispatch.ack",
  "correlation_id": "cmd-20260226-001",
  "source": "claude_code#worker-01",
  "status": "claimed",
  "task_id": "TASK-abcdef12"
}
```

---

### dispatch.noop

| Field | Detail |
|-------|--------|
| Milestone | CORE-06 |
| Type | event type |
| Description | Event generated when a `dispatch.command` times out without a corresponding `dispatch.ack`. Signals that the target agent did not respond. Contains the original `correlation_id` plus diagnostic metadata for root-cause analysis. |

**Example:**
```json
{
  "type": "dispatch.noop",
  "correlation_id": "cmd-20260226-001",
  "target": "claude_code#worker-01",
  "reason": "ack_timeout",
  "elapsed_seconds": 120,
  "task_id": "TASK-abcdef12"
}
```

---

### audience

| Field | Detail |
|-------|--------|
| Milestone | CORE-05 |
| Type | array of strings or omitted |
| Description | Optional list of agent IDs that should receive a published event. When omitted, the event is broadcast to all agents. When specified, only the listed agents see the event in their poll results. |

**Example:** `["claude_code#worker-01"]` targets a single instance; omitting `audience` broadcasts to all.

---

### audience filtering

| Field | Detail |
|-------|--------|
| Milestone | CORE-05 |
| Type | mechanism |
| Description | The event bus logic that checks whether a polling agent is included in an event's `audience` array. Events with no `audience` field pass through to all agents. Events with an `audience` array are delivered only to matching agent IDs. |

**Example:** Event with `audience: ["gemini#worker-01"]` is returned when `gemini#worker-01` polls, but not when `claude_code#worker-01` polls.

---

### event bus

| Field | Detail |
|-------|--------|
| Milestone | CORE-05 |
| Type | infrastructure |
| Description | The append-only log of collaboration events that agents poll for new messages. Supports `orchestrator_publish_event` for writing and `orchestrator_poll_events` for reading. Events are ordered by insertion sequence and retrieved via cursor-based pagination. |

**Example:** Manager publishes a `dispatch.command` to the bus; worker polls and receives it on next `orchestrator_poll_events` call.

---

### cursor

| Field | Detail |
|-------|--------|
| Milestone | CORE-05 |
| Type | integer |
| Description | An offset pointer into the event bus indicating the last event an agent has consumed. Each agent maintains its own cursor. Polling returns events from the cursor position forward. |

**Example:** Agent cursor at 42 means the next poll returns events starting at offset 43.

---

### auto_advance

| Field | Detail |
|-------|--------|
| Milestone | CORE-05 |
| Type | boolean |
| Default | true |
| Description | When `true`, the agent's stored cursor automatically advances to the end of the returned events after a poll. When `false`, the cursor stays put and the same events are returned on the next poll. Useful for at-least-once processing when the agent needs to confirm handling before advancing. |

**Example:** With `auto_advance: true`, polling events 43-50 moves the cursor to 50. With `auto_advance: false`, cursor stays at 42 and events 43-50 are returned again on next poll.
